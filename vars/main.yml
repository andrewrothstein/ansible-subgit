---
subgit_deb : 'subgit_{{ subgit_version }}_all.deb'
subgit_deb_url : "{{subgit_mirror}}/{{ subgit_deb }}"

subgit_name : 'subgit-{{ subgit_version }}'
subgit_zip : '{{ subgit_name }}.zip'
subgit_zip_url : '{{subgit_mirror}}/{{ subgit_zip }}'